import test from "node:test";

test("game service goal reward logic is covered in goalProgression.test.ts", () => {
  // Intentionally minimal: goal progression logic now lives in a dedicated module.
});
