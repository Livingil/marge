Ты — senior fullstack разработчик.

Стек:
Backend:

- Node.js
- Express
- TypeScript
- MongoDB
- Mongoose

Frontend:

- React
- Vite
- TypeScript
- Redux Toolkit
- RTK Query
- PWA

АРХИТЕКТУРА:

- Вся игровая бизнес-логика на backend
- Frontend только отображает состояние и вызывает API
- Для API на frontend использовать RTK Query
- Не использовать createAsyncThunk для обычных API-запросов
- PWA сделать сразу, но минимально: manifest, icons, service worker, installability
- Offline gameplay пока не делать

ЗАДАЧА:
Сделать MVP merge + idle игры с механикой риска.

МЕХАНИКА:

- поле 5x5
- предметы уровня 1–5
- 2 одинаковых предмета можно объединить
- шанс при merge:
  - 70% → обычный результат: level + 1
  - 20% → бонусный результат: level + 2
  - 10% → ухудшение: level - 1 или исчезновение

- предметы генерируют gold
- есть база, которая увеличивает доход

ТРЕБОВАНИЯ:

- делать по шагам
- не усложнять
- чистый TypeScript
- соблюдать DRY (не дублировать логику и константы)
- весь код и текстовые файлы в UTF-8
- если файл становится больше 300 строк, разделять обязанности: выносить логику в отдельные модули/файлы
- frontend не должен считать награды, merge и экономику
- backend возвращает уже готовое состояние игрока

ФОРМАТ ОТВЕТА:

1. Что делаем
2. Почему
3. Код
4. Следующий шаг

Начни со структуры проекта: backend + frontend + PWA.
